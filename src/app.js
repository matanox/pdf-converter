// Generated by CoffeeScript 1.6.3
'use strict';
var app, authorization, cluster, env, errorHandling, express, forkClusterWorkers, fs, host, http, logging, nconf, numCPUs, path, port, primus, routes, selfMonitor, startServer;

fs = require('fs');

nconf = require('nconf');

nconf.argv().env().file({
  file: 'loggingConf.json'
});

nconf.defaults({
  host: 'localhost'
});

express = require('express');

routes = require('../routes');

http = require('http');

path = require('path');

errorHandling = require('./errorHandling');

authorization = require('./authorization');

logging = require('./util/logging');

logging = require('./util/logging');

host = nconf.get('host');

port = process.env.PORT || 3080;

cluster = require('cluster');

numCPUs = require('os').cpus().length;

forkClusterWorkers = function() {
  var cpu, firstFork, workers, _i;
  logging.logGreen("" + numCPUs + " CPUs detected on host");
  workers = numCPUs;
  logging.logGreen("Spawn " + workers + " cluster workers...");
  for (cpu = _i = 1; 1 <= workers ? _i <= workers : _i >= workers; cpu = 1 <= workers ? ++_i : --_i) {
    cluster.fork();
  }
  firstFork = true;
  cluster.on('listening', function(worker, address) {
    var testFile, testUrl;
    if (env !== 'production') {
      testFile = 'gender differences 2013';
      logging.logGreen("node.js cluster worker " + worker.id + " now sharing on cluster listening port (pid " + worker.process.pid + ")");
      if (firstFork) {
        firstFork = false;
        testUrl = 'http://localhost' + ':' + port + '/handleInputFile?localLocation=' + testFile;
        return http.get(testUrl, function(res) {
          return logging.logBlue('Cluster response to its own synthetic client is: ' + res.statusCode);
        });
      }
    }
  });
  return cluster.on('exit', function(worker, code, signal) {
    return logging.logRed("node.js cluster worker " + worker.id + " exited (pid " + worker.process.pid + ")");
  });
};

if (cluster.isMaster) {
  logging.logGreen("Local cluster starting in mode " + env);
  logging.logGreen('Using hostname ' + nconf.get('host'));
  logging.logGreen('Using port ' + port);
  forkClusterWorkers();
} else {
  app = express();
  env = app.get('env');
  logging.init();
  if (env !== 'production') {
    primus = require('./primus/primus');
  }
  app.set('port', port);
  app.set('views', __dirname + '/views');
  app.set('view engine', 'ejs');
  app.use(express.favicon());
  if (env === 'production') {
    app.use(express.logger('default'));
  } else {
    app.use(express.logger('dev'));
  }
  app.use(express.bodyParser());
  app.use(express.methodOverride());
  app.use(express.cookieParser('93AAAE3G205OI33'));
  app.use(express.session());
  app.use(app.router);
  app.use(express["static"](path.join(__dirname, 'public')));
  app.use(errorHandling.errorHandler);
  app.get('/handleInputFile', require('../src/core/handleInputFile').go);
  startServer = function() {
    var server;
    server = http.createServer(app);
    server.timeout = 0;
    return server.listen(app.get('port'), function() {
      return logging.logGreen('cluster worker listening on port ' + app.get('port') + '....');
    });
  };
  startServer();
  selfMonitor = require('./selfMonitor').start();
}
